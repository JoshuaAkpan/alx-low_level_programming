#include "main.h"

/**
 * mul - a function that multiplies two integers.
 * @a: parameter one
 * @b: parameter two
 *
 * Return: int
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
