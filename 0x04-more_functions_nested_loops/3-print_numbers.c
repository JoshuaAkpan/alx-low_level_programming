#include "main.h"

/**
 * print_numbers - a function that prints the numbers
 *
 * Return: Always 0 (Success)
 */
void print_numbers(void)
{
	int a;

	for (a = '0'; a <= '9'; a++)
		_putchar(a);
	_putchar('\n');
}
